
    .travel-step-1 { left: 43%; top: 21%; }
    .travel-step-2 { left: 21%; top: 42%; }
    .travel-step-3 { left: 43%; top: 65%; }
    .travel-step-4 { left: 30%; top: 88%; }
  }

  @media (prefers-reduced-motion: reduce) {
    .travel-dot { display: none; }
    #travel-path-progress { display: none; }
    .travel-step { opacity: 1 !important; transform: none !important; }
  }

  .dresscode { text-align: center; border-top: 1px solid #e3ddd0; }
  .dresscode h2 { font-size: 1.7rem; margin-bottom: 1.1rem; }
  .dresscode-palette { display: flex; justify-content: center; gap: 0.7rem; margin-bottom: 1.3rem; }
  .dresscode-palette .swatch { width: 32px; height: 32px; border-radius: 50%; border: 1px solid #d8d2c4; }
  .dresscode-palette .swatch-black { background: #171412; }
  .dresscode-palette .swatch-offwhite { background: #faf8f4; }
  .dresscode-note { font-style: italic; color: #7a746a; font-size: 0.9rem; margin-bottom: 0; }
  .rsvp { background: #faf8f4; border-top: 1px solid #e3ddd0; border-bottom: 1px solid #e3ddd0; text-align: center; }
  .rsvp h2 { font-size: 1.9rem; margin-bottom: 0.8rem; }
  .deadline { color: #4a4540; }
  .deadline strong { font-weight: 600; }
  .counter {
    display: inline-block;
    margin: 0 0 1.8rem;
    padding: 0.5rem 1.1rem;
    border: 1px solid #d8d2c4;
    border-radius: 999px;
    font-size: 0.88rem;
    letter-spacing: 0.03em;
    color: #4a4540;
    background: #ffffff;
  }
  .rsvp-card { text-align: left; background: #ffffff; border: 1px solid #e3ddd0; padding: 1.8rem 1.5rem; margin-top: 1rem; }
  .field { margin-bottom: 1.3rem; }
  .field label { display: block; font-size: 0.82rem; text-transform: uppercase; letter-spacing: 0.1em; color: #57524a; margin-bottom: 0.4rem; }
  .field input[type=text], .field textarea, .field select {
    width: 100%;
    font-family: inherit;
    font-size: 1rem;
    padding: 0.6rem 0.7rem;
    border: 1px solid #cfc8b9;
    background: #fffdfa;
    color: #171412;
  }
  .field textarea { resize: vertical; min-height: 4.5rem; }
  .attend-toggle { display: flex; gap: 0.6rem; flex-wrap: wrap; }
  .attend-toggle button {
    flex: 1 1 auto;
    padding: 0.75rem 0.8rem;
    border: 1px solid #171412;
    background: #ffffff;
    color: #171412;
    font-family: 'EB Garamond', serif;
    font-size: 0.98rem;
    cursor: pointer;
  }
  .attend-toggle button.selected { background: #171412; color: #ffffff; }
  .guest-row { border: 1px solid #e3ddd0; padding: 0.8rem 0.8rem 0.9rem; margin-bottom: 0.7rem; background: #fffdfa; }
  .guest-row-head { display: flex; gap: 0.5rem; align-items: center; }
  .guest-row-head input { margin: 0; flex: 1 1 auto; }
  .guest-row .remove-guest {
    flex: 0 0 auto;
    border: 1px solid #cfc8b9; background: #fff; width: 2.75rem; height: 2.75rem; cursor: pointer; color: #57524a; font-size: 1.1rem; line-height: 1;
  }
  .guest-menu-grid { display: grid; grid-template-columns: 1fr; gap: 0.55rem; margin-top: 0.7rem; }
  .guest-menu-field label {
    display: block; font-size: 0.68rem; text-transform: uppercase; letter-spacing: 0.08em;
    color: #7a746a; margin-bottom: 0.25rem;
  }
  .guest-menu-field select { margin: 0; }
  .add-guest {
    display: inline-block; margin-top: 0.2rem; background: none; border: none; color: #171412;
    text-decoration: underline; cursor: pointer; font-family: inherit; font-size: 0.95rem; padding: 0;
  }
  .submit-btn {
    width: 100%; margin-top: 0.6rem; padding: 0.9rem; background: #171412; color: #fff; border: none;
    font-family: 'Playfair Display', serif; letter-spacing: 0.04em; font-size: 1rem; cursor: pointer;
  }
  .submit-btn:disabled { opacity: 0.5; cursor: default; }
  .form-note { font-size: 0.85rem; color: #7a746a; margin-top: 0.8rem; text-align: center; }
  .confirm-card { text-align: center; background: #ffffff; border: 1px solid #e3ddd0; padding: 2.2rem 1.5rem; margin-top: 1rem; }
  .confirm-card h3 { font-family: 'Playfair Display', serif; font-size: 1.4rem; margin-bottom: 0.6rem; }
  .edit-link { background: none; border: none; text-decoration: underline; color: #57524a; cursor: pointer; font-family: inherit; font-size: 0.92rem; margin-top: 0.8rem; }
  .signoff { text-align: center; border-top: 1px solid #e3ddd0; }
  .signoff > p { font-family: 'Playfair Display', serif; font-style: italic; font-size: 1.3rem; color: #171412; margin-bottom: 2rem; }
  .admin-link { background: none; border: none; color: #a39c8d; font-size: 0.78rem; letter-spacing: 0.08em; text-transform: uppercase; cursor: pointer; font-family: inherit; padding: 0.7rem; }
  .admin-gate { max-width: 320px; margin: 1.2rem auto 0; text-align: left; }
  .admin-gate input { width: 100%; padding: 0.5rem 0.6rem; border: 1px solid #cfc8b9; font-family: inherit; }
  .admin-gate button { margin-top: 0.6rem; width: 100%; padding: 0.6rem; background: #171412; color: #fff; border: none; cursor: pointer; }
  .admin-table { margin-top: 1.6rem; text-align: left; font-size: 0.92rem; }
  .admin-summary { display: flex; gap: 0.8rem; flex-wrap: wrap; margin-bottom: 1.2rem; }
  .admin-summary span { border: 1px solid #e3ddd0; padding: 0.4rem 0.7rem; border-radius: 999px; color: #4a4540; }
  .admin-row { border-top: 1px solid #ece7db; padding: 0.7rem 0; }
  .admin-row .r-name { font-weight: 600; }
  .admin-row .r-no { color: #a39c8d; font-style: italic; }
  .admin-row .r-guests { color: #4a4540; font-size: 0.88rem; margin-top: 0.2rem; }
  .admin-row .r-note { color: #7a746a; font-size: 0.85rem; margin-top: 0.2rem; font-style: italic; }
  .admin-row .delete-response {
    display: block; margin-top: 0.5rem; padding: 0.4rem 0;
    background: none; border: none; color: #8a2b2b; font-size: 0.8rem;
    text-decoration: underline; cursor: pointer; font-family: inherit;
  }
  .admin-row .delete-response:disabled { opacity: 0.5; cursor: default; }
  .admin-empty { color: #a39c8d; font-style: italic; }
  .error-msg { color: #8a2b2b; font-size: 0.88rem; margin-top: 0.6rem; }

  .intro-overlay {
    position: fixed; inset: 0; z-index: 9999;
    background: #171412;
    display: flex; align-items: center; justify-content: center;
    opacity: 1; transition: opacity 0.6s ease;
  }
  .intro-overlay.is-fading { opacity: 0; }
  .intro-scene {
    display: flex; flex-direction: column; align-items: center;
    cursor: pointer; perspective: 1600px; padding: 1.5rem;
  }
  .intro-scene:focus-visible { outline: 1px solid #cfc8b9; outline-offset: 8px; }
  .book {
    position: relative;
    width: min(80vw, 320px);
    aspect-ratio: 3 / 4;
    transform-style: preserve-3d;
    animation: bookPulse 1.4s ease-in-out infinite;
  }
  .book.is-open { animation: none; }
  .book-page {
    position: absolute; inset: 0;
    background: #faf8f4; border: 1px solid #d8d2c4; border-radius: 4px;
    box-shadow: 0 20px 44px rgba(0,0,0,0.45);
    display: flex; flex-direction: column; align-items: center; justify-content: center;
    text-align: center; padding: 1.4rem;
    z-index: 1;
  }
  .book-page-eyebrow {
    te